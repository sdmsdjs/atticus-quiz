[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[build.environment]
  VITE_BASE_PATH = "/"

[[redirects]]
  from = "/api/deploy-quiz"
  to = "/.netlify/functions/deploy-quiz"
  status = 200
